on:
  workflow_dispatch:
  push:
    branches:
      - 'deploy'

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with: 
          version: 1.2.313

      - name: Install R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.2.2'
        
      - name: Install System Dependecies
        run: |
          sudo apt-get update
          sudo apt-get install libcurl4-openssl-dev

      - name: Install R Dependencies
        uses: r-lib/actions/setup-renv@v2
        with:
          cache-version: 1
  
      - name: Render
        run : |
          quarto render

      - name: Deploy
        run: |
          npm install netlify-cli
          netlify deploy --dir=docs --alias=test --site=${{ secrets.NETLIFY_TOKEN }} --auth=${{ secrets.NETLIFY_TOKEN }}
